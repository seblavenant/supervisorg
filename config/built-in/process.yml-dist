logicalGroups:
    applications:
        defaultView: true
        icon: desktop
        regex: "(?P<groupValue>.+)--(?P<process>.+)"
    animals:
        icon: object-ungroup
        regex: "(.+)--(?P<groupValue>.+)-(?P<process>.+)"