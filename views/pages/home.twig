{% extends 'layout/content.twig' %}

{% block mainTitle %}All processes{% endblock %}

{% block controlPanel %}
    {{ macros.controlPanel(
        path('process.startAll'),
        path('process.stopAll')
    ) }}
{% endblock %}